# STEP Exports Required

STEP files are not included yet.

Suggested routes:
1. Open or rebuild the source in FreeCAD and export STEP.
2. Rebuild the model in Fusion or Onshape using the OpenSCAD source and dimensions as reference.
3. Export one STEP file per designed part.
